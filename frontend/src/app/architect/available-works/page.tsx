import { ArchitectAvailableWorks } from "@/components/workspace/architect-available-works";

export const metadata = {
  title: "Architect Available Works",
};

export default function ArchitectAvailableWorksPage() {
  return <ArchitectAvailableWorks />;
}
