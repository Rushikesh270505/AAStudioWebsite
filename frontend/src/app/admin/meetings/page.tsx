import { AdminMeetings } from "@/components/workspace/admin-meetings";

export const metadata = {
  title: "Admin Meetings",
};

export default function AdminMeetingsPage() {
  return <AdminMeetings />;
}
