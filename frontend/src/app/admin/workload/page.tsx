import { AdminWorkload } from "@/components/workspace/admin-workload";

export const metadata = {
  title: "Admin Workload",
};

export default function AdminWorkloadPage() {
  return <AdminWorkload />;
}
