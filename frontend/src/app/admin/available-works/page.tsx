import { AdminAvailableWorks } from "@/components/workspace/admin-available-works";

export const metadata = {
  title: "Available Works",
};

export default function AdminAvailableWorksPage() {
  return <AdminAvailableWorks />;
}
