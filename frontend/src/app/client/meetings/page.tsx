import { ClientMeetings } from "@/components/workspace/client-meetings";

export const metadata = {
  title: "Client Meetings",
};

export default function ClientMeetingsPage() {
  return <ClientMeetings />;
}
